
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>

#include <sys/time.h>


void init (void);

void LifeGame(void);

void SearchMap (void);
void CalcDoA (void);
void SummaryNext (void);


int main (void)
{
	init ();
	
	return 0;
}


// 입력파일을 받아 Life Game을 초기화 시킨다
void init (void)
{
	char * tmp;
	
	tmp = malloc (sizeof (100));
	
	tmp = gets(tmp);
	
	printf("%s", tmp);
}


// 입력된 차수만큼 Life Game을 실행한다
void LifeGame (void)
{
}


// 변경 리스트를 따라 한 차수의 맵을 갱신한다
void SearchMap (void)
{
}


// 한 세포의 생존상태를 갱신하고, 그에 따른 변경 맵, 리스트를 갱신한다
void CalcDoA (void)
{
}


// 맵에서 이번 차수에 변경된 내용을 종합 상태에 반영한다
void SummaryNext (void)
{
	
}

